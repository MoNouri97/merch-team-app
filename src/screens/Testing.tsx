import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

interface Props {}

const Testing: React.FC<Props> = ({}) => (
	<View>
		<Text>Hello From Testing</Text>
	</View>
);
const styles = StyleSheet.create({});
export default Testing;
